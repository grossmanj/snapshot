<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Customer Snapshot – 15-Second Briefing</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body { background-color: #f5f7fb; }
        .card { border: none; box-shadow: 0 1rem 1.5rem rgba(0,0,0,0.05); }
        .kpi-value { font-size: 1.5rem; font-weight: 700; }
        .pill { border-radius: 50px; padding: .25rem .75rem; font-size: .875rem; }
        .timeline:before { content: ''; position: absolute; left: 18px; top: 0; bottom: 0; width: 2px; background: #dee2e6; }
        .timeline-item { position: relative; padding-left: 48px; margin-bottom: 1rem; }
        .timeline-item:before { content: ''; position: absolute; left: 10px; top: 6px; width: 16px; height: 16px; border-radius: 50%; background: #0d6efd; }
        .cta-card { background: linear-gradient(135deg, #0d6efd, #6610f2); color: #fff; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom mb-3">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/">Customer Snapshot</a>
            <span class="text-muted">15-Second Briefing</span>
        </div>
    </nav>
    <div class="container mb-5">
        @RenderBody()
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
